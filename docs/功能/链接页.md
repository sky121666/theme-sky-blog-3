# 链接页

## 结论

链接页已经是独立 app，不再复用普通 browser/explorer 窗口。

当前协议固定为：

- `pageApp = links`
- `pageMode = browser-links`
- `windowVariant = links`

它承接 4 类能力：

1. 友链分组浏览
2. PluginLinks RSS 朋友圈
3. 留言板查看
4. 友链申请 / 管理员直连添加

## 我想改什么，先看哪里

| 需求 | 先看 |
| --- | --- |
| 改 links 页面协议、标题、窗口参数 | [/templates/links.html](/templates/links.html) |
| 改 links 页面结构和留言/提交模板 | [/templates/modules/links-app/list.html](/templates/modules/links-app/list.html) |
| 改 links 运行时交互、分组/RSS 切换、提交逻辑 | [/src/apps/links/runtime.js](/src/apps/links/runtime.js) |
| 改 links 样式和移动端窗口表现 | [/src/apps/links/styles/index.css](/src/apps/links/styles/index.css) |
| 改 links 窗口外壳 | [/templates/modules/links-app/window.html](/templates/modules/links-app/window.html) |

## 入口与目录

路由入口：

- [/templates/links.html](/templates/links.html)

模板模块：

- [/templates/modules/links-app/list.html](/templates/modules/links-app/list.html)
- [/templates/modules/links-app/window.html](/templates/modules/links-app/window.html)

源码目录：

- [/src/apps/links/entry.js](/src/apps/links/entry.js)
- [/src/apps/links/runtime.js](/src/apps/links/runtime.js)
- [/src/apps/links/protocol.js](/src/apps/links/protocol.js)
- [/src/apps/links/styles/index.css](/src/apps/links/styles/index.css)

## 页面结构

links 页固定分成 Finder 式侧栏和主内容区：

1. 左侧侧边栏
   - 全部友链
   - 朋友圈
   - 友链分组
   - RSS 分组与来源
   - 添加友链
   - 留言板
2. 右侧主内容区
   - 友链卡片列表
   - RSS 动态列表与游标续载
   - 搜索 / 排序
   - 留言板
   - 提交弹窗

这套结构已经包含在 links 自己的窗口里，不应该再回退成“复用别的 browser 窗口内容”。

## 后台配置与注解

links 页当前涉及两条配置链：

### 1. 主题设置

配置位置：

- [/settings.yaml](/settings.yaml)

字段：

- `links.profile.display_name`

作用：

- 配置“全部友链”默认标题
- 同时影响页面主标题、侧边栏标题和窗口标题回退值

### 2. LinkGroup 注解

配置位置：

- [/annotation-setting.yaml](/annotation-setting.yaml)

对象：

- `LinkGroup`

字段：

- `icon`

作用：

- 友链分组侧边栏图标
- 分组标题图标

留空时回退到主题内置默认图标。

## 提交与留言流程

当前完全按 `PluginLinks 2.2.1` 官方能力分成两条安全路径，不依赖 `plugin-friends` 或 `link-submit`。

### 1. 已登录管理员

- 先读取 Halo 当前用户，再请求 PluginLinks 受保护的链接管理列表确认权限。
- 有权限时使用官方 detail 与 RSS discovery 接口识别站点信息。
- 新增友链通过 Halo 标准 `core.halo.run/v1alpha1/links` 资源接口写入，RSS 地址映射到 `spec.rss`，写入请求携带 Halo 当前会话的 CSRF 令牌。
- 修改申请不直接覆盖现有数据，仍生成可审核的留言内容。
- 权限失效或接口失败时说明具体原因，并降级到留言申请。

### 2. 访客申请

- 官方插件没有匿名创建接口，因此访客不会请求任何受保护的后台写入端点。
- 浏览器以 `credentials: omit` 直接读取目标站公开 HTML；受目标站 CORS、HTTPS 混合内容和内容类型规则限制。
- 识别成功只填充表单，不额外弹出成功提示；失败会说明跨域、非 HTML、超时或后台权限原因，并保留网址供手动填写。
- 完成后生成 Markdown、复制并切换到 PluginLinks 留言板，由管理员审核处理。

## 朋友圈与 RSS

- 统一入口为 `/links?view=friends`，不再维护 `/friends` 页面和独立 Friends app。
- 首屏使用 `linkFeedFinder.list({limit: 20})`，来源树使用 `linkFeedFinder.groupBy(1)`。
- 客户端续载只请求匿名公共 `/apis/api.link.halo.run/v1alpha1/linkfeeds`，不携带 Halo 登录态。
- 支持 `groupName`、`linkName` 和 `beforePublishedAt + beforeId`；分组与单来源参数不会同时发送。
- RSS 公共能力关闭时显示明确空态；切换、刷新和续载不使用骨架屏，更新期间保留现有列表并轻量淡化。
- `PluginLinks` 契约和严格 inventory 均固定到官方稳定版 `2.2.1`。

相关接口写在：

- [/src/apps/links/runtime.js](/src/apps/links/runtime.js)

验证入口为 `pnpm run verify:links`；真页交互同时纳入 `pnpm run smoke:playwright`。

## 移动端规则

links 页移动端已经做成沉浸式窗口：

- 隐藏 menubar
- 隐藏 dock
- 隐藏 traffic lights
- 隐藏 resize handle
- 窗口铺满 `100dvh`
- 主内容区可滚动

如果你看到手机端又出现 menubar / dock，优先检查：

- [/src/apps/links/styles/index.css](/src/apps/links/styles/index.css)

## 什么时候改这里

这些需求优先改 links app：

1. 友链分组卡片布局
2. RSS 朋友圈、来源筛选与继续加载
3. links 页面搜索 / 排序
4. 添加友链弹窗与权限降级
5. 留言板兜底逻辑
6. links 移动端窗口表现

不要把 links 逻辑塞回通用 browser/explorer。
