# 项目进度

## 说明

这份文档只保留高信号状态：

1. 当前主线在做什么
2. 下一批准备做什么
3. 已经稳定交付了什么

README 不再承载大段实现清单。

## 进行中

- [ ] 样本待补
  - Moments 代码块样本仍缺，补齐后运行 `pnpm run verify:moments`
  - Docsme 代码块、KaTeX / Mermaid 样本仍缺，补齐后运行 `pnpm run verify:docsme`
  - Docsme 1.7.0 核心项目/正文/目录 surface 已通过；权限态、多语言和多版本 surface 无真实样本，testedVersion 仍按 1.6.0 记录
  - Shiki 1.4.1、KaTeX 3.0.0、Text Diagram 1.5.2 的实际插件资源 fixture 已通过；这里缺的是站点真实内容的跨页面证据
  - 样本内容参考 [/docs/测试样本数据.md](/docs/测试样本数据.md)

- [ ] 可选产品决策
  - Equipment 是否进入组件中心仍待判断，有明确展示价值再做
  - 预览资源、壁纸、配色预览卡只在视觉目标明确时继续校准
  - Photos REST/tag/ungrouped 已评估，当前无真实样本收益，暂不实现

- [ ] 长期观察项
  - 插件脚本已确认通过 `<halo:footer />` 全局注入；主题已记录资源数量/体积预算，不删除 footer 或正则过滤未知插件标签，后续优先推动插件自身页面级 gating
  - Douban 只在插件升级、封面代理配置变化或体验增强时重新评估
  - 后续只按审计证据做最小修复，不做无证据重构

## 待开始

当前没有必须启动的新功能批次。

## 已完成

- [x] PluginLinks 微信桌面端式链接应用初版（2026-07-22 验证完成）
  - `/links` 默认收窄为 `60px` 功能栏加 `440px` 会话列表；链接详情、RSS 动态、添加助手和留言板按需展开右侧详情，桌面端不替换左侧列表
  - `/links?link=`、`?view=friends&scope=all|unread|favorite|later`、`?view=friends&linkName=`、`&itemId=`、`?view=apply`、`?view=board` 使用明确 history 状态；移动端使用列表到全屏详情的返回路径
  - 单一 RSS 来源详情包含真实头像、名称、简介、域名和访问入口，再展示 PluginLinks 动态；来源描述统一纯文本化，不伪造点赞、评论、图片等缺失字段
  - `plugin:links:view` 权限用户自动显示独立“收藏 / 稍后阅读”功能块及未读入口；保护列表支持卡片收藏、稍后阅读、内部摘要详情、自动已读、打开后完成稍后阅读和手动设为未读，访客继续只使用公开 feed
  - 新增友链不再提供人工模式开关：`plugin:links:manage` 自动走官方识别和 Link CRUD，其余账号与修改申请自动生成留言；权限通过 Halo `uiPermissions` 精确判断
  - links app 固定使用微信绿色 `#07C160 / #95EC69 / #DFF7E8`，只跟随明暗模式，不读取主题或系统强调色；沿用现有 Thymeleaf、Tailwind、Iconify、Alpine 和原生控件，没有新增 UI 框架或 daisyUI
  - `verify:links`、`pnpm run check`、Halo Reload、全站 Playwright 和暗色真页收藏列表均通过；真实页面已只读确认 197 条未读与 1 条收藏，未在自动验收中改动现有收藏 / 稍后阅读状态

- [x] PluginLinks 2.2.1 友链、RSS 朋友圈与自助提交重写（2026-07-22 验证完成）
  - `/links` 使用微信桌面端式布局并整合“全部友链 / RSS 朋友圈 / 收藏 / 稍后阅读 / 留言板”；朋友圈入口为 `/links?view=friends`，支持 RSS 分组、单来源筛选、`beforePublishedAt + beforeId` 游标续载、刷新和浏览器 history，不再维护 `/friends` 独立 App
  - 朋友圈 SSR 与桌面小组件使用官方 `linkFeedFinder.list/groupBy`；访客客户端续载请求匿名公共 `linkfeeds`，具 `plugin:links:view` 权限的登录用户自动切换到受保护 RSS 列表和状态接口
  - 添加友链助手不再依赖 `link-submit`：访客只做无凭据浏览器识别并复制申请到留言板；已登录且 `uiPermissions` 包含 `plugin:links:manage` 的用户才调用官方 detail、RSS discovery 和标准 Link CRUD，修改申请始终保留人工审核
  - 识别成功静默填充名称、描述、Logo 与 RSS；跨域、混合内容、非 HTML、超时、权限失效和创建失败均给出对应原因。输入只接受 HTTP(S)，远端文本和 URL 在写入 DOM/载荷前统一清理
  - 删除旧 Friends 模板、运行时、路由、构建入口和产物；桌面朋友圈小组件保留历史 widget id 以兼容已保存布局，但数据模型已切换为官方 `LinkFeedItemVo`，入口统一到 `/links?view=friends`
  - 严格 inventory 从 26 个旧 fixture 收敛为 24 个当前契约插件，`PluginLinks 2.2.1 / STARTED` 精确通过；本机残留的 `plugin-friends 1.4.6` 与 `link-submit 1.0.7` 虽为 `FAILED`，已不再影响主题路由、Finder、构建或门禁
  - `pnpm run check`、`verify:links`、13 路由 Reload、24 场景 Playwright、10 页真实审计、HTML 性能、冷 PJAX 资产和 42 次 PJAX 生命周期全部通过；Playwright 的 pageerror / console error / request failure 均为 0，10 页 LCP 平均 `550ms`、最高 `836ms`，CLS 最高 `0.0013`，TBT 平均 `2.8ms`、最高 `28ms`
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发版

- [x] 友链分组状态、访客前端识别与视觉收口（2026-07-21 验证完成）
  - `/links` 的“全部友链”计数固定为完整集合，不再随搜索、分组或留言板切换变化；空分组使用独立提示，非法 `group` 自动回退并清理 URL，插件返回的描述标签文本在展示和搜索前统一转为纯文本
  - 分组切换改为 `pushState`，浏览器前进/后退会恢复当前分组；留言板使用 `?view=board` 明确深链，刷新和分享后仍停留在留言板，同时保留其他查询参数
  - 添加友链助手对所有访客开放公开元数据识别：浏览器直接读取目标 HTML 的标题、简介、Logo、RSS 与 generator，不携带 Halo 登录态，也不再调用管理员 Console detail API
  - CORS、HTTPS 混合内容、非 HTML、超时和超大响应均停止识别，保留网址并给出手动补充提示；连续识别支持取消，切换新网址后不会残留上一站点的自动填充数据，远端 HTML 只解析为纯文本/安全 URL
  - 侧栏选中态、行高和间距已对齐归档的轻量 Finder 风格；搜索/排序/活动项/弹窗补齐无障碍名称与状态，减少动态效果时关闭平滑滚动、弹窗和卡片位移动画
  - `pnpm run check`、`pnpm run verify:link-submit`、13 路由 reload 与全站 Playwright 均通过；真页覆盖计数、描述清理、分组 history、非法/空分组、留言板刷新深链、公开 HTML 识别、非 HTML 手动兜底，`/links` 与 `links-interactions` 均为 0 pageerror / console error / request failure
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发版

- [x] 标签统一 Finder、全部文章分页与无骨架 PJAX 适配（2026-07-21 验证完成）
  - `/tags` 现在直接使用“标签 / 文章 / 预览”Finder：左栏输出唯一“全部标签”入口和完整扁平标签列表，中栏默认展示全部已发布文章，右栏默认预览第一篇；不再自动进入首个标签
  - 全部文章分页使用 `/tags?page=N` 并沿用 `site.post.tagPageSize`；标签详情继续使用 Halo 原生 `/tags/{slug}/page/N`，根页、详情、直达、PJAX 和浏览器后退均保持明确 URL 与稳定文章集合
  - 根页和详情共用标签侧栏、文章行与预览片段；详情切换不再替换左栏，当前标签使用 `aria-current` 标识并自动滚入侧栏可视区，同时保留文章计数；空标签和越界页提供返回根页、第一页或最后有效页的真实链接
  - 标签侧栏已读取主题 `icon` 注解和 Halo 原生 `spec.color`；自定义 SVG、颜色图标及默认 Lucide 回退在根页/详情共用，视觉采用与归档一致的 15px 裸图标，不增加底块或边框，白色标签色在浅色模式下使用轻量图形阴影保持可辨识
  - 旧前端 REST、固定 20 条、`sessionStorage` 缓存、批量 HTML 拼接、按钮过滤和局部列表骨架均已移除；同应用标签切换只显示 3px 轻量进度，当前 Finder 保留到响应完成，标签前端 JS 构建体积由约 11KB 降至约 1.7KB
  - 标签路由收敛为配置的根路径、单标签和单标签 `/page/N`；非法 `/tags/page/N`、多余路径、0/01/非数字页码和旧 `/tag/*` 不再被误判为标签 App
  - Finder ≥940px 使用 `220px / minmax(420px, 1fr) / 300–340px` 三栏，641–939px 使用双栏并隐藏预览，≤640px 使用单栏；详情移动端标签列表改为可横向滚动的稳定标签带，并通过 `min-width: 0` 防止长列表把工作区撑宽
  - 标签静态专项、Reload、标签真页专项与全站 Playwright 均通过；真页覆盖全部文章 query 分页、`tag-vjieJ` 原生分页、PJAX 无窗口骨架、history、直达、越界恢复、1280/700/390 三档布局、移动端 44px 触控目标及控制台/pageerror
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发版

- [x] 图库窗口越界、稳定侧栏与详情胶片条修复（2026-07-21 验证完成）
  - 图库列表、分组和精选集不再为所有照片持续设置 `view-transition-name`；只有用户点击且完整位于滚动可视区的单张照片，在列表进入详情时使用临时 `photos-active-photo` 共享过渡；离屏照片不再生成根级快照，消除 420ms 动画期间窗口外闪现
  - 列表和详情共用唯一的“图库 / 精选集 / 相簿”导航 fragment；从相簿进入详情后侧栏宽度、位置、选中项和滚动位置保持不变，返回按钮通过 `backUrl` 保留 `group/page/size`
  - 旧“附近照片”侧栏已移除；详情主区改为中性画布和底部胶片条，胶片条使用 `photoFinder.list(page,size,group)` 输出当前分页完整照片列表，当前项自动居中，可直接点击切换
  - 上一张、下一张、左右方向键和胶片条使用同一路由与 PJAX 生命周期；首尾禁用状态补充 `aria-disabled/tabindex`，放大后拖拽监听不再抢占链接或按钮点击
  - 详情内切换只为主照片建立方向感共享过渡：旧图在 140ms 内淡出，新图短暂延迟 40ms 后用 200ms 柔和进入，位移限制为 6px；侧栏、标题栏、工具栏和胶片条保持原位，当前胶片项使用克制的透明度与 `1.025` 缩放反馈
  - 底部胶片条进一步改为悬浮半透明胶片坞：桌面细指针空闲约 3.2 秒后柔和下沉隐藏，移动指针、滚轮、触摸或键盘输入会立即唤醒；显现使用 220ms 渐变和 340ms 弹性位移，隐藏使用 360ms 渐进透明度和 400ms 渐进位移，两者采用对称 `cubic-bezier(0.33, 0, 0.67, 1)`，下沉收敛到 6px、缩放收敛到 `0.992`，避免透明度在退场前段过早耗尽
  - 胶片坞只为桌面自动隐藏场景预提升 `transform/opacity` 合成层，不动画 blur、filter、阴影或尺寸；悬停和键盘聚焦期间保持展开，隐藏时同步设置 `inert/aria-hidden`，触屏设备保持常显，减少动态效果时直接完成显隐
  - 胶片坞显隐不再占用主区网格行，主舞台与主照片在显示、隐藏前后位置和尺寸保持一致；详情 PJAX 切换会清理旧节点的计时器、监听器与状态，并让新页面胶片坞重新短暂显示
  - 胶片坞活动监听范围由整个图库窗口收窄到主图内容区，标题栏、侧栏及绿色最大化按钮不再误唤醒；最大化/还原的 300ms 窗口尺寸过渡期间会锁定隐藏态并即时停止胶片坞自身过渡，消除主图底部短暂遮挡
  - 详情切到大图时不再等待 Alpine 才补主图居中：基础 CSS 从 PJAX 新 DOM 首帧起就提供 `translate(-50%, -50%) scale(1)`；详情内切图和列表进入详情期间，胶片栏均使用独立 `photos-detail-filmstrip` 命名过渡层（`z-index: 2`），稳定压在主图过渡层（`z-index: 1`）上方，消除中央缩略图被大图快照短暂盖住的问题
  - 列表中后段照片进入详情时，PJAX 会在目标快照前同步把 `.is-current` 缩略图滚入中心；自动化夹具使用第 10–14 项且要求 `scrollLeft > 1`，并锁定新快照到过渡结束的滚动偏差不超过 `1px`，避免修复层级后出现胶片栏收尾跳变
  - 目标原图预载和 live `decode()` 均限制在 200ms 且支持导航取消；减少动态效果时不创建预载图片或 View Transition，连续三次切换通过全局结算屏障只提交首个与最终意图，过期解码不会再发送交换事件或覆盖最新标题/计数
  - `pnpm run check`、`pnpm run verify:reload`、`verify:photos:view-transition`、`verify:pjax-cold-assets`和全站 `smoke:playwright` 均通过；真页退场 `180ms` 中间态实测透明度 `0.501`、下沉约 `2.55px` 且仍为 `visible`，最大化时间轴 `0 / 80 / 180 / 320 / 1000ms` 均保持隐藏且透明度为 `0`；手工真页逐帧采样确认详情切图 `11–279ms` 内胶片层始终高于主图层，列表进入详情 `195ms` 中段同样无遮挡且 `scrollLeft` 全程为 `255`；自动化继续覆盖 opacity、translateY、scale 单调中间态、两类切图新快照首帧居中、胶片命名层层级/唯一性/清理和滚动幂等，并覆盖分组 17 张胶片条、第 1/17、11/17、17/17 边界、1.5倍缩放后切换、空闲/唤醒/悬停/聚焦/触屏/减少动态效果、最大化/还原、实际 old/new 动画名称与时序、解码超时/取消、三连切换、信息/评论抽屉及 PJAX 清理
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发版

- [x] 分类统一 Finder、全部文档分页与响应式浏览修复（2026-07-21 验证完成）
  - `/categories` 直接使用“分类 / 文档 / 预览”Finder：左栏只输出一棵完整分类树和唯一“全部分类”入口，中栏默认展示全部已发布文档，右栏默认预览第一篇；旧分类卡片概览、重复分类区块和第二套 REST 数据通道均已移除
  - 全部文档分页使用 Halo 可渲染的 `/categories?page=N` 查询路由，并沿用 `site.post.categoryPageSize`；具体分类继续使用 Halo 原生 `/categories/{slug}/page/N`，两套分页不再混用，直达、PJAX、前进和后退均保持文档集合及选中状态正确
  - 分类选择和两套分页的同应用 PJAX 等待态不再显示窗口骨架：响应返回前保留当前 Finder，仅显示 3px 轻量进度并维持 `aria-busy`，完成后原子替换内容；分类进入正文等跨应用导航仍保留窗口骨架
  - 根页和分类详情共用文档行、预览面板及分类树视觉契约；空分类和越界分页保持完整工作区，并提供返回根页、第一页或最后有效页的真实链接
  - 分类树已读取主题 `icon` / `color` 注解；自定义 SVG、元数据颜色及默认文件夹回退在根页/详情共用，图标尺寸和行内布局对齐归档侧栏，并通过契约禁止恢复额外包裹底块
  - Finder 内容宽度 ≥940px 使用 `220px / minmax(420px, 1fr) / 300–340px` 三栏，641–939px 使用双栏并隐藏预览，≤640px 使用单栏与横向分类条；1280×720、700×900、390×844 均无横向溢出，移动端关键触控目标 ≥44px
  - `pnpm run check`、13 路由 reload、分类真页专项和全站 Playwright 均通过；专项使用延迟请求验证分类选择、两套分页及前进后退等待期间无骨架且进度/busy 状态完整，并覆盖唯一分类树、默认预览、分页去重、直达、越界恢复、三档响应式及控制台/pageerror
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发版

- [x] 归档年月层级与月内分页修复（2026-07-20 验证完成）
  - 年份/月索引已与路由分页结果拆开：根归档默认保留全部年份，年份路由展示对应月份，月份路由只展示该月文章；原工作区外层全局“上一页/下一页”已移除
  - 路由合同收敛为归档根、年份、月份和月份 `/page/N`；全局 `/archives/page/N` 与年份 `/archives/{year}/page/N` 不再进入新 PJAX 合同，避免把全局/年份页码误当作月页码
  - 月内“继续加载”遵循 Halo 的 `site.post.archivePageSize` 和原生 path 路由；分页深链采用累计语义，`/archives/2025/03/page/2` 可直接恢复前两页共 14 篇，加载后刷新、打开文章再返回均不丢失已加载文章
  - 年份和月份的同应用 PJAX 等待态不再显示窗口骨架：旧年月索引与文章区保留到响应完成，仅显示 3px 轻量进度并维持 `aria-busy`；归档进入正文仍按跨应用导航显示窗口骨架
  - 已补重复文章防护、重复 next URL 防护、HTTP 重试、AbortController/generation 过期响应隔离、PJAX dispose、加载状态播报、焦点转移和移动端 44px 触控目标
  - `pnpm run verify:archive-pagination`、`pnpm run check`、13 路由 reload、归档真页专项、归档性能预算、全站 Playwright 与 42 次 PJAX 生命周期均通过；真页专项使用延迟请求验证年/月切换无骨架、跨应用骨架仍在，并覆盖后退前进、直接 page2、文章返回、非法月、越界页和 390px 移动端
  - 当前 51 篇样本下根归档 138.8 KB / gzip 24.4 KB、TTFB 约 20ms，月 page2 171.2 KB / gzip 30.3 KB、TTFB 约 24ms，均低于 512 KB raw / 100 KB gzip 门禁；Halo 2.25 没有年月聚合计数 Finder，完整准确索引仍随文章总数线性增长，文章规模显著增大时需改为服务端聚合 API 或有界批量索引
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发版

- [x] 最新稳定插件与前端生命周期适配（2026-07-20 验证完成）
  - Halo 最低要求保持 `>=2.25.0`，在 Halo 2.25.4 完成 13 组 reload、26 条 Playwright 路由场景（25×200 + 1×预期 404）、10 页真实审计、性能与 42 次 PJAX 生命周期回归
  - 严格 inventory 覆盖 26 个主题已知前台插件；`PluginLinks` 按用户决定精确固定 `2.0.0`，guard 会阻止缺失、停用、非 `STARTED`、版本漂移和误升，其他插件按最新稳定版适配或兼容测试
  - `verify:plugins:all` 会先执行完整工程检查和重新构建，再把已启动插件路由设为必达，并使用明确的本地 Halo 地址执行真页、性能与 PJAX 门禁，避免旧产物或 404 被误判为通过
  - 修复 app-loader/PJAX 的过期意图、重复加载、超时和历史状态竞态；非首页直达后返回首页改为安全 JSON 协议 hydration，42 次导航中主文档请求与 shell-core CSS 重载均为 0
  - 修复桌面布局保存快照与 URL 安全、通知中心过期点击、Bangumis/Equipment 销毁后旧响应、Explorer 批量渲染与 Photos/Douban/Moments 的请求取消和生命周期清理；首页 Finder 查询只在需要时执行
  - 修复 link-submit 失败后手动申请消失/复制旧 Markdown、Moments/Friends 旧分页响应串页、Douban/Photos 筛选或销毁后旧请求追加、Photos 错误误判为到底、Docsme hash 更新覆盖 PJAX history state、PluginFeed 未安装时暴露失效 RSS 链接
  - Moments/Friends 已接入可取消分页与局部 Shiki 1.4.1 增量渲染；Contact Form、Hyperlink Card、Lottery、Restricted Reading、Vote、AI Assistant 的全局资源单实例和只读展示生命周期已纳入真页门禁
  - Steam 1.0.0、link-submit 1.0.7、Docsme 1.7.0 核心 surface、Shiki 1.4.1、KaTeX 3.0.0、Text Diagram 1.5.2、SEO Tools 1.9.5、LightGallery 1.2.1、Online 1.0.5 已完成专项验证
  - `package.json` 的 14 个直接依赖已是最新稳定版；生产 31 条、完整 181 条依赖图均为 0 漏洞，许可证与第三方声明门禁通过；GitHub 仓库变量 `NPM_AUDIT_ALLOWED=true` 已确认
  - 编辑器样本有 4 张正文图片仍指向不可达的旧局域网地址；仅对这 4 个精确 `192.168.1.23:8090/upload/` URL 记录内容告警，其他连接错误仍会阻塞门禁，内容数据未越权修改
  - 本批只提交并推送 `main`；不修改 CI/CD，不提高主题版本，不打标签、不创建 Release、不发布应用市场

- [x] v0.9.42 发布门禁
  - 发布提交 `4dabf6b` 的原有 CI `29701069178` 已通过；CI/CD 工作流未改动
  - GitHub Release `v0.9.42` 已公开，原有 CD `29701129881` 的打包、Release 资产上传与 Halo 应用市场发布全部通过
  - 公开 ZIP 含 266 个条目、546806 bytes，SHA-256 为 `305919c879f3600677fa87bb0fa396443fce5967f1ec3ac59cdd5be19e7fb3f5`，下载后复验通过
  - Halo 应用市场版本 `app-release-kgaurm4g` 已标记为最新，版本 `0.9.42`、最低 Halo 要求 `>=2.25.0`
  - 上一稳定版 `v0.9.40` 及其 ZIP 保持公开且完整，可作为回滚基线；`v0.9.41` 草稿保持未公开且未改写

- [x] v0.9.41 阶段：修复实现与本地门禁（未公开）
  - Dock/Base64 图标导致首页 HTML 膨胀的问题已通过 URL 白名单和设置说明收口
  - Moments、Photos、Equipment 已接入缩略图、响应式图片和 PJAX dispose
  - 搜索 observer、窗口 resize/MQL/ResizeObserver、评论懒加载、浮动滚动条和完整/同 variant PJAX 资源生命周期已补清理契约
  - 直接依赖已升级并精确锁定；本地 Node/pnpm 验证、许可证、第三方声明和 pnpm 供应链策略已建立，CI/CD 工具版本保持原配置
  - 漏洞审计发现并修复 `lodash@4.17.23` 的高危代码注入漏洞，传递依赖已升级到 `4.18.1`，生产与完整依赖图均为 0 条漏洞
  - Halo 最低版本只在 `theme.yaml` 声明为 `>=2.25.0`；实际适配、插件 inventory、运行态和路由验证以最新稳定版 2.25.4 为准，不再维护最低版运行矩阵
  - CI/CD 保持项目原有流程，不因 Halo 最低版本声明增加双版本容器、草稿恢复或自定义发布编排

- [x] 架构与构建链收口
  - 已完成 `shell-core / apps / widgets / shared` 目录重构
  - Vite 已切到多入口 + code-splitting
  - 构建输出治理与冲突副本清理已接入
  - `features/*` 已彻底退出页面 runtime 主链路

- [x] 桌面壳层主链稳定
  - 首页桌面态、主窗口、Dock、Header、PJAX 已稳定接通
  - 浏览器历史导航标题恢复和 same-variant 前进回退问题已修复
  - 移动端 browser/explorer/author 的沉浸式边界已统一
  - `window-frame / window-titlebar / leading / actions` 已抽成公共 primitive

- [x] 独立功能页稳定
  - 图库、瞬间、浏览列表、阅读页、认证页、链接页、追番、豆瓣、Steam、装备、Docsme 都已进入独立 app 目录
  - `protocol / state / hydrate / documentState / dispose` 已接入主链路
  - 阅读页已补齐目录、点赞、上下篇和评论区排版
  - 链接页已补齐独立窗口、分组图标、自助提交插件适配与留言兜底
  - 追番页已补齐 Bilibili Bangumi 插件模板、筛选、滚动加载分页和列表卡片
  - Steam 页已补齐资料、最近游玩、游戏库、徽章和热力图容器
  - 装备页已补齐分组导航、沉浸式单品展厅和自动加载
  - Docsme 页已补齐项目大厅、文档正文、目录页、自定义主题 DOM、主题色、暗亮色和 `/docs/**` 同窗口 PJAX
  - 豆瓣页已补齐书影音归档、类型/状态/题材筛选、搜索、网格/列表、Quick Look 和移动端全屏 App 模式

- [x] 工程护栏建立
  - 已补 `lint / typecheck / smoke / check`
  - 已补 PR / Push CI
  - 本地 `pnpm run check` 已能完整通过
  - 关闭缓存的 `Playwright smoke` 已接入并可自动发现真实详情页

- [x] 后台设置与文档体系建立
  - 顶栏、Dock、瞬间、桌面、小组件等设置已接入后台
  - links 已接入独立标题配置，LinkGroup 已接入图标元数据
  - 文档已拆为：索引 / 架构 / 设置 / 功能 / 专题
  - README 已收成项目入口页，不再承载长进度日志
  - 已补充桌面小组件协议与未来插件存储接口文档
  - 已补代码质量评估文档

- [x] 构建产物路径收口
  - 公共壳层已统一到 `shell-core`
  - 页面入口产物已按 `apps/<page>/index` 落盘
  - chunks 已按 `apps / shared / shell-core / widgets` 分类

- [x] 豆瓣收藏小组件
  - 已新增 `plugin-douban.showcase` large 大卡片组件
  - 支持真实插件 API 数据、类型/状态配置、自动轮播和悬停预览
  - 已补 memory + sessionStorage 缓存，避免页面切换时重复请求
  - 已修复桌面 PJAX 往返后组件 DOM 重置、缩略图轨道丢失和桌面图标偶发硬跳转

- [x] 图片懒加载基础能力落地
  - 主题可控范围内的图片已统一补 `loading=\"lazy\" + decoding=\"async\"`
  - `data-src` 图片已统一走 `IntersectionObserver`
  - 正文内容图片也能在运行时补齐懒加载属性

- [x] 真实页面审计与增强评估
  - 已补 `pnpm run audit:real-pages`，用于生成真实页面性能和插件资源加载报告
  - 审计报告已补资源大小、图片 lazy 比例和非预期资源判定
  - Photos 公共 REST/tag/ungrouped 已完成只读评估，当前维持 SSR/Finder 实现
  - `verify:moments` / `verify:docsme` 已补 skipped/failed 中文排查提示和报告字段

- [x] 插件技能资料整理
  - 已补充独立 skill：
    - Steam
    - Comment Widget
    - Search Widget
    - Shiki
    - Moments
    - Links
    - Friends
    - Photos
    - Equipment
    - Douban
    - Bilibili Bangumi
    - Docsme
    - Auth Passkey
