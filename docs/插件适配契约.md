# 插件适配契约

## 目的

这里记录主题和 Halo 插件之间的前台契约版本。

“契约版本”不是当前站点安装的插件版本，也不是本地技能记录的最新插件版本，而是主题模板、运行时和样式实际按哪个插件版本的路由、模板变量、Finder、API、DOM 片段和前端生命周期编写。

如果无法证明某个插件当时按哪个版本适配，就标记为 `unconfirmed`，不要猜版本。只有在存在提交时间、技能更新时间、旧文档或明确需求文本互相印证时，才允许标记为 `inferred`，并且必须保留“推断”状态。

## 状态定义

| 状态 | 含义 | 是否允许写入模板版本 |
| --- | --- | --- |
| `confirmed` | 有文档、提交或验证记录能证明契约版本 | 是 |
| `compatible-tested` | 契约版本可能较旧，但某个新版本已经实际测试通过 | 是，同时记录已测试版本 |
| `inferred` | 版本由提交时间、技能更新时间、旧文档或需求文本推断得出，但缺少直接验收记录 | 是，但必须同时写 `status: inferred` |
| `split-contract` | 同一插件不同页面或能力按不同时间/字段适配，需要拆分 surface 记录 | 只在具体 surface 行里写版本 |
| `unconfirmed` | 主题已适配该插件，但无法证明契约版本 | 否 |
| `not-adapted` | 主题明确不适配该插件 | 否 |

## 版本字段

| 字段 | 含义 |
| --- | --- |
| 契约版本 | 主题实现写代码时依赖的插件能力版本 |
| 已测试版本 | 当前主题实际验证通过的插件版本 |
| 最新版状态 | 是否已经跟踪和评估插件最新版本 |
| 适配提交 | 能定位主题适配代码的主要提交 |
| 依据 | 文档、提交、验证报告或明确用户决策 |

## 当前矩阵

| 插件 | 主题能力 | 契约版本 | 已测试版本 | 最新版状态 | 状态 | 适配提交 | 依据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `plugin-bilibili-bangumi` | `/bangumis`、`/bangumis/page/{page}`、类型/状态筛选、滚动加载 | `1.4.0` | `1.4.1` | 最新稳定版 `1.4.1` 已完成默认页、筛选模型、分页入口和非法页码 404 回归 | `compatible-tested` | `e140415`、本轮复验 | 主题继续消费 1.4.0 路由模型；Playwright 已验证 1.4.1 对非法 `/bangumis/page/not-a-number` 返回 404，未发现主题破坏性变化 |
| `plugin-docsme` | `/docs`、`/docs/**`、项目大厅、正文页、目录页、目录抽屉、同 App PJAX、SEO 与评论容器 | `1.6.0` | `1.7.0` | 最新稳定版 `1.7.0` 的核心项目/正文/目录 surface 与富内容运行时已通过 | `compatible-tested` | `799ba9e`、`e387495`、本轮适配 | 主题继续消费 1.6.0 已确认的数据契约，已在 1.7.0 上完成核心页面兼容回归；本行不包含权限、多语言和多版本状态 |
| `plugin-docsme` | 项目权限、多语言、多版本切换 | `1.6.0` | `1.6.0` | 当前安装 `1.7.0`，但本地没有相应权限/语言/版本内容样本，不能把该 surface 升记为 1.7.0 testedVersion | `confirmed` | `799ba9e`、既有契约 | 主题保留插件生成的 permalink 和切换器字段；补齐真实样本并通过切换与权限矩阵后再更新 testedVersion |
| `PluginPhotos` | `/photos` 列表、分页、分组、桌面图库小组件 | `2.1.1` | `2.1.2` | 最新稳定版 `2.1.2` 的列表、相簿、分组和真实数据入口已通过 | `compatible-tested` | `4d17bf9`、`5a7e959`、本轮复验 | 2.1.2 未改变主题消费的 Finder/SSR 列表契约；Playwright 与 reload 已覆盖列表分面 |
| `PluginPhotos` | `/photos/{name}` 详情、邻近照片、EXIF、标签、评论抽屉、macOS 详情视图 | `2.1.1` | `2.1.2` | 最新稳定版 `2.1.2` 的真实照片详情、详情图像和返回链路已通过 | `compatible-tested` | `5a26aa7`、本轮复验 | 详情继续消费 `PhotoVo`、`neighbors`、`photoUrl.detail(...)`、`backUrl`、`exif` 和 `spec.tags`；浏览器烟测已自动发现并打开真实详情 |
| `PluginMoments` | `/moments`、`/moments/{name}`、动态列表、详情评论、作者页动态引用、桌面瞬间小组件 | `1.16.0` | `1.16.1` | 最新稳定版 `1.16.1` 的列表、真实详情、公开 API 与已审核评论路径已通过 | `compatible-tested` | `55e3b77`、`ed95857`、`40de71d`、本轮复验 | 缺失作者与 RSS 是插件自身边缘能力；主题消费的列表、详情、媒体、统计和评论字段已在 1.16.1 上回归 |
| `plugin-friends` | `/friends`、朋友圈 feed、RSS 聚合展示 | `1.4.6` | `1.4.6` | 1.4.6 已复验；主题侧 SSR 路由、`linkName` 筛选、`friendFinder` 和 HTML 下一页加载不变，依赖 `PluginLinks >=2.0.0` | `confirmed` | `615051f`、本轮复验 | 1.4.6 主要跟随 Links 2.0 依赖与管理端 Link 选择 API；主题仍消费 `friends.items`、`friends.nextUrl` 和 `FriendPostVo.spec` 字段 |
| `PluginLinks` | `/links`、分组友链、留言板、友链申请助手 | `2.0.0` | `2.0.0` | 按用户决定固定 `2.0.0`，不跟随 `2.2.1`；避免与其他插件契约冲突 | `confirmed` | `a06e4da`、`3bef922`、`5cc2bd4`、本轮适配 | 审计脚本已加入精确 pin 门禁；主题继续使用 `api.link.halo.run` / `console.api.link.halo.run` 命名空间和 2.0.0 路由模型 |
| `link-submit` | 友链自助提交 API、失败后留言兜底、修改申请复制到留言板 | `1.0.7` | `1.0.7` | 最新稳定版 `1.0.7` 的 200/400/409/429/503 状态语义与留言兜底已完成非写入验证 | `confirmed` | `5cc2bd4`、本轮适配 | 为避免创建真实申请，本轮使用纯契约测试覆盖成功、校验冲突、限流和服务异常；Markdown 复制与留言板兜底保持可用 |
| `halo-plugin-steam` | `/steam`、`/steam/page/{page}`、资料、游戏库、徽章、热力图容器 | `1.0.0` | `1.0.0` | 最新稳定版 `1.0.0` 的缺配置、API 失败、空缓存、缺封面、下架游戏、分页和 VO 字段已完成适配 | `confirmed` | `ffbf2bb`、本轮适配 | 页面与小组件共用 1.0.0 数据归一化；支持 `headerImageUrl`/`realHeaderImage`、格式化/原始游玩时长、`delisted` 禁用态和请求取消 |
| `equipment` | `/equipments`、`/equipments/page/{page}`、分组导航、装备展厅、自动加载 | `1.1.0` | `1.1.1` | `1.1.1` 新增公开 REST API，但当前主题仍使用 SSR 路由模型、`equipmentFinder` 和 HTML 下一页加载，不接入公共 API | `compatible-tested` | `d563907`、`e387495` | 用户确认主题契约按 5 个月前的 `1.1.0` 记录；本地安装 `equipment` 1.1.1，新增 `/apis/api.equipment.kunkunyu.com/v1alpha1/equipments` 和 `/equipmentgroups` 对当前主题不是必要适配 |
| `PluginCommentWidget` | 文章、页面、瞬间、图库、Docsme、友链留言评论 | `3.1.1` | `3.1.1` | 最新稳定版 `3.1.2` 的 JS/CSS 和 init 脚本已在真实文章返回 200；因未执行登录、提交、待审、错误和多端组件状态矩阵，保守保留 3.1.1 testedVersion | `confirmed` | 多处接入、本轮只读资源核验 | 3.1.2 未改变主题使用的 `<halo:comment>` subject 契约；当前资源加载和无控制台错误不能替代组件展开后的专项验收 |
| `PluginSearchWidget` | 顶栏 `SearchWidget.open()` 搜索入口、快捷键唤起、搜索弹框样式变量 | `1.7.1` | `1.7.1` | 1.7.1 契约已确认；插件无前台路由、Finder 或设置页，主题只调用官方全局 API 并适配 CSS 变量 | `confirmed` | `835bd73` | 本地安装 `PluginSearchWidget` 1.7.1 已启动；技能基线为 1.7.1，主题使用 `pluginFinder.available('PluginSearchWidget')` 和 `SearchWidget.open()`，搜索请求仍由插件委托 Halo Core Search Index API |
| `auth-passkey` | 登录页 Passkey 表单片段兼容、`halo-form` 容器保留、remember-me 和验证码区域协作 | `1.0.4` | `1.0.4` | 1.0.4 契约已确认；主题不实现 WebAuthn 流程，只兼容插件注册的 AuthProvider 和 `login_passkey.html` 表单片段 | `confirmed` | `e7b304c` | 用户确认当前适配版本为 `1.0.4`；技能基线为 1.0.4，主题登录片段保留 `.halo-form` 和动态 `fragmentTemplateName`，让 Passkey 插件接管登录表单内容 |
| `plugin-shiki` | 文章/页面代码高亮、PJAX、暗色切换兼容 | `1.4.1` | `1.4.1` | 最新稳定版 `1.4.1` 的普通代码、超长行、成对/尾部折叠、Shadow DOM、明暗切换和资源加载已通过 | `confirmed` | 本轮 Shiki 适配与浏览器 fixture | 验证直接加载本机 1.4.1 插件资源；Docsme/Moments 仍缺站点真实代码内容样本，但不再把“无内容”误判为运行时未适配 |
| `plugin-katex` | Docsme 行内/块级公式、预渲染保护、客户端冷补载 | `3.0.0` | `3.0.0` | 最新稳定版 `3.0.0` 的实际插件资源和 KaTeX `0.16.25` 渲染通过；冷补载 URL 已固定版本查询 | `confirmed` | 本轮 Docsme 适配 | 支持 `.katex-inline`、`.katex-block`、`math-inline`、`math-display`；已渲染 DOM 不重复处理，失败时保留可读源码；版本化冷补载门禁已通过 |
| `text-diagram` | Docsme Mermaid 图表、客户端冷补载、明暗主题重绘 | `1.5.2` | `1.5.2` | 最新稳定版 `1.5.2` 的实际 Mermaid 资源、单 SVG 和明暗重绘通过；冷补载 URL 已固定版本查询 | `confirmed` | 本轮 Docsme 适配 | 消费 `text-diagram[data-type="mermaid"][data-content]`，防止重复渲染并在不可用时保留源码；版本化冷补载门禁已通过 |
| `seo-tools` | 插件优先 SEO、客户端缺失补齐、语义去重、PJAX head 同步 | `1.9.5` | `1.9.5` | 最新稳定版 `1.9.5` 的客户端 title/meta/canonical/Open Graph/Twitter/JSON-LD 协作已通过 | `confirmed` | 本轮 SEO 适配 | 原始服务端 HTML 由插件负责；主题 JavaScript 启动后再补配置允许的缺失项并去重，保留多个 `og:image`、`article:tag` 和 JSON-LD，PJAX 不重复克隆重叠 selector |
| `PluginLightGallery` | 文章图片灯箱、冷 PJAX 资源加载、实例销毁/重挂载 | `1.2.1` | `1.2.1` | 最新稳定版 `1.2.1` 的冷加载、三轮往返和零实例泄漏已通过 | `confirmed` | 本轮运行时适配 | 主题只管理 `#article-content`，不销毁 Photos/Moments 自有预览；导航代次与 rAF 取消避免旧页面重新挂载 |
| `plugin-online` | 页面在线监测、PJAX 路径注册、私密页语义与 history 恢复 | `1.0.5` | `1.0.5` | 最新稳定版 `1.0.5` 的单 WebSocket、完整注册序列与前进/后退私密状态恢复已通过 | `confirmed` | 本轮运行时适配 | 只解析真实 `__ONLINE_MONITOR_META__` 内联脚本；目标页私密状态写入 history state，并在 capture `popstate` 阶段先于插件重连恢复 |
| `plugin-douban` | `/douban`、`/douban/page/{page}`、类型/状态/题材筛选、搜索、网格/列表、Quick Look、桌面收藏小组件 | `1.2.5` | `1.2.5` | 1.2.5 契约已确认；真实数据下类型/状态/题材筛选、搜索、分页、封面、空状态和桌面小组件已确认 | `confirmed` | `92b8990` | 技能基线和插件源码确认 `DoubanMovie.spec/faves` 字段、`type/status/genre/keyword` 查询参数和 `types/genres` 接口；主题已新增 `templates/douban.html`、`src/apps/douban` 与 `plugin-douban.showcase` 小组件；用户已手动确认 v0.9.34 `/douban` 真实数据表现正常，v0.9.35 桌面小组件 PJAX 往返和会话缓存已自动化验证 |
| `plugin-forum` | 论坛页面 | 无 | 无 | 不适配 | `not-adapted` | 无 | 用户已确认本主题不适配论坛插件 |

## 当前收口结论

当前已纳入主题的插件页面和组件均已记录契约版本、已测试版本、状态、模板注释和依据；没有 `unconfirmed` 的已适配插件。

## 2026-07-20 运行时版本扫描

扫描命令为 `pnpm run audit:plugins ...`。结果来自本机 Halo `2.25.4` 的插件 API；`installed` 只说明当前启用版本，不能自动覆盖 `contractVersion` 或 `testedVersion`。

| 插件 | sourceBaseline | installed / phase | contractVersion | testedVersion | 本轮判断 |
| --- | --- | --- | --- | --- | --- |
| `plugin-bilibili-bangumi` | `1.4.1` | `1.4.1 / STARTED` | `1.4.0` | `1.4.1` | 默认页、筛选、分页入口与非法页码 404 已复验 |
| `plugin-douban` | `1.2.5` | `1.2.5 / STARTED` | `1.2.5` | `1.2.5` | reload 与 Playwright 已复验 |
| `equipment` | `1.1.1` | `1.1.1 / STARTED` | `1.1.0` | `1.1.1` | 默认页、分组与 Playwright 已复验 |
| `steam` | `1.0.0` | `1.0.0 / STARTED` | `1.0.0` | `1.0.0` | 1.0.0 页面、分页、异常与 VO 专项适配通过 |
| `link-submit` | `1.0.7` | `1.0.7 / STARTED` | `1.0.7` | `1.0.7` | 200/400/409/429/503 非写入专项验证通过 |
| `PluginPhotos` | `2.1.2` | `2.1.2 / STARTED` | `2.1.1` | `2.1.2` | 列表、相簿、分组与真实详情通过 |
| `PluginMoments` | `1.16.1` | `1.16.1 / STARTED` | `1.16.0` | `1.16.1` | 列表、详情、公开 API 与评论样本通过 |
| `PluginLinks` | `2.2.1` | `2.0.0 / STARTED` | `2.0.0` | `2.0.0` | 用户指定兼容 pin；禁止跟随 2.2.1 |
| `plugin-friends` | `1.4.6` | `1.4.6 / STARTED` | `1.4.6` | `1.4.6` | 版本对齐，路由与来源筛选通过 |
| `plugin-docsme` | `1.7.0` | `1.7.0 / STARTED` | `1.6.0` | 核心 surface `1.7.0`；权限/语言/版本 surface `1.6.0` | 核心项目、真实正文、PJAX、SEO、评论和富内容已回归；高级状态等待真实样本 |
| `PluginCommentWidget` | `3.1.2` | `3.1.2 / STARTED` | `3.1.1` | `3.1.1` | 组件专项状态矩阵待补 |
| `PluginSearchWidget` | `1.7.1` | `1.7.1 / STARTED` | `1.7.1` | `1.7.1` | 四层版本对齐；搜索监听生命周期已收口 |
| `shiki` | `1.4.1` | `1.4.1 / STARTED` | `1.4.1` | `1.4.1` | 实际插件资源的代码、长行、fold、明暗和 Shadow DOM fixture 通过 |
| `auth-passkey` | `1.0.4` | `1.0.4 / STARTED` | `1.0.4` | `1.0.4` | 四层版本对齐；未执行真实凭据绑定/删除 |
| `plugin-katex` | `3.0.0` | `3.0.0 / STARTED` | `3.0.0` | `3.0.0` | 实际插件资源、行内/块级公式和失败回退通过 |
| `text-diagram` | `1.5.2` | `1.5.2 / STARTED` | `1.5.2` | `1.5.2` | 实际 Mermaid 资源、明暗重绘与重复执行通过 |
| `seo-tools` | `1.9.5` | `1.9.5 / STARTED` | `1.9.5` | `1.9.5` | 插件原始服务端输出优先；主题客户端补齐、去重和 PJAX 同步通过 |
| `PluginLightGallery` | `1.2.1` | `1.2.1 / STARTED` | `1.2.1` | `1.2.1` | 冷 PJAX 资源、挂载/销毁与无泄漏通过 |
| `plugin-online` | `1.0.5` | `1.0.5 / STARTED` | `1.0.5` | `1.0.5` | 单连接、路径注册与 history 私密语义通过 |

Halo 最低版本 `>=2.25.0` 只作为主题安装限制；插件版本结论以当前最新稳定版 Halo `2.25.4` 的精确 inventory 与各自专项证据为边界。

剩余事项分两类：

1. Docsme 仍缺 1.7.0 权限/多语言/多版本样本，Docsme/Moments 仍缺站点真实 KaTeX、Mermaid、代码块内容样本；实际插件资源 fixture 已通过，因此这是 surface testedVersion 与跨内容证据缺口，不是当前核心运行时适配失败。
2. `plugin-douban` 已确认到 `1.2.5`；Photos 的 tag/ungrouped 与 Equipment 小组件仍是可选产品增强，不进入本轮兼容修复。

## 模板注释规范

对应插件模板只写 HTML 注释，不在未知版本时写版本属性。`inferred` 可以写入具体版本，但必须保留 `status: inferred`，不能升级成 `confirmed`。

已确认契约：

```html
<!-- plugin-contract: plugin-bilibili-bangumi; contract-version: 1.4.0; tested-version: 1.4.1; status: compatible-tested; source: docs/插件适配契约.md -->
```

未知契约：

```html
<!-- plugin-contract: PluginPhotos; contract-version: pending; status: unconfirmed; source: docs/插件适配契约.md -->
```

推断契约：

```html
<!-- plugin-contract: PluginLinks; contract-version: 2.0.0; status: inferred; source: docs/插件适配契约.md -->
```

分面契约：

```html
<!-- plugin-contract: PluginPhotos; surface: detail; contract-version: 2.1.1; status: confirmed; source: docs/插件适配契约.md -->
```

如果以后验证了新版本兼容：

```html
<!-- plugin-contract: PluginPhotos; contract-version: pending; tested-version: 2.1.1; status: compatible-tested; source: docs/插件适配契约.md -->
```

## 更新流程

1. 更新插件或适配主题前，先查本文件。
2. 对比插件字段、路由、Finder、API、模板变量和 PJAX 生命周期变化。
3. 修改主题后记录验证命令和验证结果。
4. 更新本文件的契约版本、已测试版本、状态和依据。
5. 只有 `confirmed`、`compatible-tested` 或 `inferred` 才允许在模板注释里写具体版本；`inferred` 必须保留推断状态。
6. 同一插件存在不同 surface 时，不要用一个版本覆盖全部能力，按页面或能力拆行记录。
