# 插件适配契约

## 目的

这里记录主题和 Halo 插件之间的前台契约版本。

“契约版本”不是当前站点安装的插件版本，也不是本地技能记录的最新插件版本，而是主题模板、运行时和样式实际按哪个插件版本的路由、模板变量、Finder、API、DOM 片段和前端生命周期编写。

如果无法证明某个插件当时按哪个版本适配，就标记为 `unconfirmed`，不要猜版本。只有在存在提交时间、技能更新时间、旧文档或明确需求文本互相印证时，才允许标记为 `inferred`，并且必须保留“推断”状态。

## 状态定义

| 状态 | 含义 | 是否允许写入模板版本 |
| --- | --- | --- |
| `confirmed` | 有文档、提交或验证记录能证明契约版本 | 是 |
| `compatible-tested` | 契约版本可能较旧，但某个新版本已经实际测试通过 | 是，同时记录已测试版本 |
| `inferred` | 版本由提交时间、技能更新时间、旧文档或需求文本推断得出，但缺少直接验收记录 | 是，但必须同时写 `status: inferred` |
| `split-contract` | 同一插件不同页面或能力按不同时间/字段适配，需要拆分 surface 记录 | 只在具体 surface 行里写版本 |
| `unconfirmed` | 主题已适配该插件，但无法证明契约版本 | 否 |
| `not-adapted` | 主题明确不适配该插件 | 否 |

## 版本字段

| 字段 | 含义 |
| --- | --- |
| 契约版本 | 主题实现写代码时依赖的插件能力版本 |
| 已测试版本 | 当前主题实际验证通过的插件版本 |
| 最新版状态 | 是否已经跟踪和评估插件最新版本 |
| 适配提交 | 能定位主题适配代码的主要提交 |
| 依据 | 文档、提交、验证报告或明确用户决策 |

## 当前矩阵

| 插件 | 主题能力 | 契约版本 | 已测试版本 | 最新版状态 | 状态 | 适配提交 | 依据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `plugin-bilibili-bangumi` | `/bangumis`、`/bangumis/page/{page}`、类型/状态筛选、滚动加载 | `1.4.0` | 待记录 | 待检查 | `confirmed` | `e140415` | [追番文档](功能/追番.md) 明确写明来自 `plugin-bilibili-bangumi` 1.4.0 路由模型 |
| `plugin-docsme` | `/docs`、`/docs/**`、项目大厅、正文页、目录页、版本/语言切换、目录抽屉 | `1.6.0` | 待记录 | 待检查 | `confirmed` | `799ba9e`、`e387495` | [Docsme 文档](功能/Docsme.md) 明确记录 1.6.0 边界 |
| `PluginPhotos` | `/photos` 列表、分页、分组、桌面图库小组件 | 待确认 | `2.1.0` | 列表、分页、分组和详情入口 smoke 已通过；公共 REST/tag/ungrouped 待评估 | `compatible-tested` | `4d17bf9`、`5a7e959` | 本地安装 `PluginPhotos` 2.1.0 已启动，`/photos`、`/photos?page=1&size=20`、`/photos?view=albums`、分组页和详情入口均正常；列表仍按旧实现追溯，契约版本不倒填 |
| `PluginPhotos` | `/photos/{name}` 详情、邻近照片、EXIF、标签、评论抽屉、macOS 详情视图 | `2.1.0` | `2.1.0` | 详情路由和 2.1.0 模型 smoke 已通过；细节体验继续按主题需求打磨 | `compatible-tested` | `5a26aa7` | 详情页改造发生在 Photos 技能更新后，且本地 2.1.0 详情页返回 `data-photos-view="detail"`、邻近照片和 EXIF 区域 |
| `PluginMoments` | `/moments`、`/moments/{name}`、动态列表、详情评论、作者页动态引用、桌面瞬间小组件 | `1.6.0` | `1.16.0` | 统计字段和 PJAX smoke 已跟进，含代码块内容样本待补 | `compatible-tested` | `55e3b77`、`ed95857` | 本地安装 `PluginMoments` 1.16.0 已启动，公开 API 返回 `stats.upvote` / `stats.totalComment` / `stats.approvedComment`；主题列表已消费统计字段并补齐 AUDIO/POST 占位；PJAX 到 Moments 后 Shiki extra-path replay 无脚本残留 |
| `plugin-friends` | `/friends`、朋友圈 feed、RSS 聚合展示 | `1.4.5` | `1.4.5` | 1.4.5 契约已确认；主题使用 SSR 路由、`linkName` 筛选和 HTML 下一页加载，不改用公共 API | `confirmed` | 待追溯 | 用户确认朋友圈适配版本为 `1.4.5`；当前主题消费 `friends.items`、`friends.nextUrl`、`spec.authorUrl`、`spec.author`、`spec.logo`、`spec.title`、`spec.postLink`、`spec.description`、`spec.pubDate`、`spec.linkName`，与技能 1.4.5 契约一致 |
| `PluginLinks` | `/links`、分组友链、留言板、`link-detail` 提取 | `1.7.2` | 待记录 | 待检查 | `inferred` | `a06e4da`、`3bef922`、`5cc2bd4` | Links 技能较早且当前适配围绕已知 `PluginLinks` / `link-detail` 契约，缺少直接验收记录，因此只标记推断 |
| `link-submit` | 友链自助提交 API、失败后留言兜底、修改申请复制到留言板 | 待确认 | 待记录 | 待检查 | `unconfirmed` | `5cc2bd4` | 自助提交插件不属于 `PluginLinks` 本体，契约版本待确认 |
| `halo-plugin-steam` | `/steam`、`/steam/page/{page}`、资料、游戏库、徽章、热力图容器 | `0.3.0` | 待记录 | 待检查 | `inferred` | `ffbf2bb` | Steam 技能基线较早且适配集中在该路由模型，缺少直接验收记录，因此只标记推断 |
| `equipment` | `/equipments`、`/equipments/page/{page}`、分组导航、装备展厅、自动加载 | `1.1.0` | `1.1.1` | `1.1.1` 新增公开 REST API，但当前主题仍使用 SSR 路由模型、`equipmentFinder` 和 HTML 下一页加载，不接入公共 API | `compatible-tested` | `d563907`、`e387495` | 用户确认主题契约按 5 个月前的 `1.1.0` 记录；本地安装 `equipment` 1.1.1，新增 `/apis/api.equipment.kunkunyu.com/v1alpha1/equipments` 和 `/equipmentgroups` 对当前主题不是必要适配 |
| `PluginCommentWidget` | 文章、页面、瞬间、图库、Docsme、友链留言评论 | 待确认 | 待记录 | 待检查 | `unconfirmed` | 多处接入 | 组件级接入已完成，但当前文档未记录契约版本 |
| `PluginSearchWidget` | 顶栏 `SearchWidget.open()` 搜索入口 | 待确认 | 待记录 | 待检查 | `unconfirmed` | 待追溯 | 组件级接入已完成，但当前文档未记录契约版本 |
| `auth-passkey` | 登录/注册/重置密码页面的 Passkey 注入兼容 | 待确认 | 待记录 | 待检查 | `unconfirmed` | `e7b304c` | 兼容性验证完成，但当前文档未记录契约版本 |
| `plugin-shiki` | 文章/页面代码高亮、PJAX、暗色切换兼容 | `1.3.0` | `1.3.0` | PJAX smoke 已通过，暗色视觉待人工复核 | `compatible-tested` | 本轮 Shiki PJAX 修复 | 主题不接管渲染；文章页 PJAX 往返后 `shiki-code` 数量稳定、无嵌套；Moments/Docsme extra-path 响应脚本已在 PJAX 后主动 replay，临时脚本不残留 |
| `plugin-douban` | 豆瓣页面 | 无 | 无 | 未开始 | `not-adapted` | 无 | [插件适配状态](插件适配状态.md) 记录为未完成 |
| `plugin-forum` | 论坛页面 | 无 | 无 | 不适配 | `not-adapted` | 无 | 用户已确认本主题不适配论坛插件 |

## 模板注释规范

对应插件模板只写 HTML 注释，不在未知版本时写版本属性。`inferred` 可以写入具体版本，但必须保留 `status: inferred`，不能升级成 `confirmed`。

已确认契约：

```html
<!-- plugin-contract: plugin-docsme; contract-version: 1.6.0; status: confirmed; source: docs/插件适配契约.md -->
```

未知契约：

```html
<!-- plugin-contract: PluginPhotos; contract-version: pending; status: unconfirmed; source: docs/插件适配契约.md -->
```

推断契约：

```html
<!-- plugin-contract: PluginLinks; contract-version: 1.7.2; status: inferred; source: docs/插件适配契约.md -->
```

分面契约：

```html
<!-- plugin-contract: PluginPhotos; surface: detail; contract-version: 2.1.0; status: inferred; source: docs/插件适配契约.md -->
```

如果以后验证了新版本兼容：

```html
<!-- plugin-contract: PluginPhotos; contract-version: pending; tested-version: 2.1.0; status: compatible-tested; source: docs/插件适配契约.md -->
```

## 更新流程

1. 更新插件或适配主题前，先查本文件。
2. 对比插件字段、路由、Finder、API、模板变量和 PJAX 生命周期变化。
3. 修改主题后记录验证命令和验证结果。
4. 更新本文件的契约版本、已测试版本、状态和依据。
5. 只有 `confirmed`、`compatible-tested` 或 `inferred` 才允许在模板注释里写具体版本；`inferred` 必须保留推断状态。
6. 同一插件存在不同 surface 时，不要用一个版本覆盖全部能力，按页面或能力拆行记录。
